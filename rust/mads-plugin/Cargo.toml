[package]
name    = "mads-plugin"
version = "0.1.0"
edition = "2021"
description = "Safe Rust bindings for writing MADS plugins"
license = "MIT"

[dependencies]
serde_json = "1"

[features]
default = []
